cd /home/hron/Documents/WebGoat

# Check if Jenkinsfile is still there
ls Jenkinsfile

# If missing, create it again
nano Jenkinsfile   # paste your Jenkinsfile content

# Push it
git add Jenkinsfile
git commit -m "fix: re-add Jenkinsfile after checkout"
git push origin main
